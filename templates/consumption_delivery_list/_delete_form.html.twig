<form method="post" action="{{ path('consumption_delivery_list_delete', {'id': consumption_delivery_list.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ consumption_delivery_list.id) }}">
    <button class="btn">Delete</button>
</form>
