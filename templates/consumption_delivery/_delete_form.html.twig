<form method="post" action="{{ path('consumption_delivery_delete', {'id': consumption_delivery.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ consumption_delivery.id) }}">
    <button class="btn">Delete</button>
</form>
