{% extends 'base.html.twig' %}

{% block title %}MaterialRecord{% endblock %}

{% block body %}
    <h1>MaterialRecord</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ material_record.id }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ material_record.date ? material_record.date|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Quantity</th>
                <td>{{ material_record.quantity }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('material_record_index') }}">back to list</a>

    <a href="{{ path('material_record_edit', {'id': material_record.id}) }}">edit</a>

    {{ include('material_record/_delete_form.html.twig') }}
{% endblock %}
