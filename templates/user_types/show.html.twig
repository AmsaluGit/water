{% extends 'base.html.twig' %}

{% block title %}UserTypes{% endblock %}

{% block body %}
    <h1>UserTypes</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ user_type.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ user_type.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ user_type.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('user_types_index') }}">back to list</a>

    <a href="{{ path('user_types_edit', {'id': user_type.id}) }}">edit</a>

    {{ include('user_types/_delete_form.html.twig') }}
{% endblock %}
